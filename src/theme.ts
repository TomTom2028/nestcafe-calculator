import {createTheme} from "@mui/material";

const theme = createTheme({
    palette: {
        background: {
            default: '#FFE8D6',
        },
        text: {
            primary: '#6B705C'
        },
        primary: {
            main: '#B7B7A4',
        }
    }
})


export default theme
